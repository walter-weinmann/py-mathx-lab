"""Project-local Sphinx extensions.

The documentation uses a few tiny custom directives to bridge Markdown (MyST)
and reStructuredText (Sphinx domains). Keeping them in-tree avoids introducing
extra build dependencies.
"""
