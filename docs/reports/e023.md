# E023 — Residue class distribution $\mod {params.q}$

**Reproduce:**

```bash
make run EXP=e023
```

## Parameters
- n_max: `3000000`
- q: `10`

## Notes
- In the limit, reduced residue classes should balance, but finite ranges can show visible drift.
